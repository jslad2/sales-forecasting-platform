{
  "version": 2,
  "routes": [
    { "src": "/(.*)", "dest": "api/index.py" }
  ],
  "buildCommand": "pip install -r requirements-vercel.txt && mkdir -p .vercel/output && cp -r api templates static .vercel/output/",
  "outputDirectory": ".vercel/output",
  "includeFiles": ["templates/**", "static/**"]
}
