{
  "version": 2,
  "builds": [
    { "src": "api/index.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/index.py" }
  ],
  "buildCommand": "python3 -m pip install -r requirements-vercel.txt && mkdir -p .vercel/output && cp -r api .vercel/output/",
  "outputDirectory": ".vercel/output"
}
